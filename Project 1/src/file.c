#include "file.h"
#include <string.h>
#include <stdlib.h>
/*
struct file_class
{
    
};*/
/*
File* initialize_file_info(){
    File * f = (File *)malloc(sizeof(File));
    strcpy(f->file_name,"");
    strcpy(f->file_type,"");
    f->file_size = 0;
    strcpy(f->file_access,"");
    strcpy(f->file_created_date,"");
    strcpy(f->file_modified_date,"");
    f->md5_code = 0;
    f->sha1_code = 0;
    f->sha256_code = 0;
    
    return f;
}

void changeInfo(char *attribute, char *value){
    
}

*/